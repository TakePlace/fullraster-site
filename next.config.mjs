/** @type {import('next').NextConfig} */
const nextConfig = {
  // Modo padrão do Next.js (servidor Node), compatível com o deploy
  // "Node.js Apps" da Hostinger: builda e roda `npm start` automaticamente.
};

export default nextConfig;
